# RDP Agent — Python Dependencies
# =================================
# Install all dependencies:
#   pip install -r requirements.txt
#
# If you do NOT want ML scoring (CodeBERT) and want a faster/lighter install,
# set ml_scoring.enabled: false in config.yaml and skip torch/transformers.

# -- Core (required) --
flask>=2.0.0
flask-cors>=4.0.0
pyyaml>=6.0

# -- ML Scoring / CodeBERT (required for full ML scoring) --
# torch can be large (~2 GB on CPU build).
# For CPU-only (no NVIDIA GPU):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then run:  pip install -r requirements.txt
torch>=2.0.0
transformers>=4.30.0
